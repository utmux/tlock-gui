# TLockGUI v1.1.2

## 新功能

- 开始批处理时检测所有输入文件所在存储卷是否具有机械寻道惩罚。
- 检测到同一机械硬盘包含多个输入任务且并发大于 1 时，弹窗解释多流读取造成的磁头寻道问题。
- “使用 1（推荐）”为默认选择，只调整当前批次；高级设置中的默认并发仍保持 2。
- 高级用户仍可选择保留原并发，适用于多个独立机械硬盘等特殊场景。
- 中英文界面均已覆盖新的检测提示与日志。
